enum ApiMethod { post, get, put, delete, patch }

enum FileFormat { pdf, jpeg, png }

enum UploadType { image, file }

enum ConditionType { general, special }

enum SelectEmirates {
  abuDhabi,
  ajman,
  dubai,
  fujairah,
  rasAlKhaimah,
  sharjah,
  ummAlQuwain
}

enum SelectEducationType {
  governorateEducation,
  privateEducation,
}

enum SettingPolicy { policy, terms, aboutApp }

enum AchievementType { distinction, projects, skillsAndExperiance, socialMedia }

enum FormType { current, previous }

enum StepType { current, previous, next }

enum SubscriberType { winner, statement, participationCertificate }
enum SortType { newer, oldest }

